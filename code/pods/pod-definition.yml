apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod
  labels:
    name: myapp
    type: front-end
spec:
  containers:
    - name: myapp-container
      image: nginx
      resources:
        limits:
          memory: "128Mi"
          cpu: "500m"
      ports:
        - containerPort: 80
